import React from "react";

type Props = {
  firstName: string;
  resetPasswordToken: string;
};

export default function ResetPasswordEmailTemplate({
  firstName,
  resetPasswordToken,
}: Props) {
  return <></>;
}
